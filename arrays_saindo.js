// Montando o guarda-roupa
let guardaroupa = ["camisa marrom","calça cargo","tenis"]

// Exibindo as opcões do guarda-roupa 
console.log("meu guarda roupa tem: ", guardaroupa)

// Selecionando look

let lookSelecionando = [] // look vazio
// usando push para adiconar peças de roupas do nosso guarda-roupa no nosso look
lookSelecionando.push(guardaroupa[0]) // adicionando a peça em 0
lookSelecionando.push(guardaroupa[1]) // adicionando a peça em 2
lookSelecionando.push(guardaroupa[2]) // adicionando a peça em 3

console.log(lookSelecionando)
// Exibindo o look selecionado 
console.log("Seu look para sair é",lookSelecionando)
console.log(inventario.length)